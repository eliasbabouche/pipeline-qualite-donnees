# Execution du pipeline
pandas>=2.2
pyarrow>=17.0      # lecture et ecriture Parquet
requests>=2.32

# Les outils des etapes suivantes (pandera, dbt-duckdb, dagster) seront ajoutes
# au moment ou ils servent.

# Developpement (versions figees aussi dans la CI)
pytest==9.1.1
ruff==0.16.8
